'use strict';

describe('myApp.addBook module', function() {

  beforeEach(module('myApp.addBook'));

  describe('addBook controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('addBookCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});